# Application Configuration
APP_PORT=5000
APP_MONGO_URI="mongodb://db:db@mongo:27017/sportondb?authSource=admin"

# Environment Mode
# Options: development, production
# Default: production (if not set) - more secure by default
# - development: Relaxed CORS for local development
# - production: Strict CORS, only allows configured frontend origin
APP_ENV=development

# Security - JWT Secret
# ⚠️ IMPORTANT: Generate a secure secret using: openssl rand -hex 64
# Never share this secret and never commit the actual value to git!
APP_JWT_SECRET=your-super-secret-jwt-key-min-128-characters
